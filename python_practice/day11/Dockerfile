# 1. ベースイメージの指定（Python 3.11の軽量版を使います）
FROM python:3.11-slim

# 2. コンテナ内での作業ディレクトリ（場所）を設定します
# コンテナの中に '/app' というフォルダを作り、そこを現在地にします
WORKDIR /app

# 3. ファイルのコピー
# [PC側のファイル] を [コンテナ側の現在地(.)] にコピーします
COPY main.py .

# 4. コンテナ起動時のコマンド
# コンテナが立ち上がった瞬間に実行するコマンドです
CMD ["python", "main.py"]